# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'accelerometer3' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Firebase/Core'
pod 'Firebase/Database'

  # Pods for accelerometer3

  target 'accelerometer3Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'accelerometer3UITests' do
    # Pods for testing
  end

end
